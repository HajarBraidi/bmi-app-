<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BmiApp">
        <!-- Lab 4 -->
        <activity android:name=".BmiActivity"              android:exported="false"/>
        <activity android:name=".CurrencyConverterActivity" android:exported="false"/>

        <!-- Lab 5 -->
        <activity android:name=".AboutActivity"            android:exported="false"/>

        <!-- Lab 6 -->
        <activity android:name=".VitalActivity"            android:exported="false"/>
        <activity android:name=".RecordsActivity"          android:exported="false"/>

        <!-- Launcher principal (Lab 5) -->
        <activity android:name=".HubActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>