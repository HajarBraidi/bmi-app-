<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootRecords"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp">

    <!-- Scroll horizontal + vertical pour gérer les colonnes/longues listes -->
    <HorizontalScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/btnAddRecord"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ScrollView
            android:layout_width="wrap_content"
            android:layout_height="match_parent">

            <LinearLayout
                android:orientation="vertical"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <!-- En-tête -->
                <TableLayout
                    android:id="@+id/tableHeader"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:stretchColumns="*">

                    <TableRow
                        android:background="#E0E0E0"
                        android:padding="8dp">

                        <TextView
                            android:text="Date"
                            android:textStyle="bold"
                            android:padding="6dp"/>
                        <TextView
                            android:text="Time"
                            android:textStyle="bold"
                            android:padding="6dp"/>

                        <TextView
                            android:text="Sys"
                            android:textStyle="bold"
                            android:padding="6dp"/>

                        <TextView
                            android:text="Dia"
                            android:textStyle="bold"
                            android:padding="6dp"/>

                        <TextView android:text="HR"
                            android:textStyle="bold"
                            android:padding="6dp"/>

                        <TextView
                            android:text="Temp"
                            android:textStyle="bold"
                            android:padding="6dp"/>

                        <TextView
                            android:text="Sugar"
                            android:textStyle="bold"
                            android:padding="6dp"/>

                    </TableRow>
                </TableLayout>


                <!-- Corps de la table (rempli en Kotlin) -->
                <TableLayout
                    android:id="@+id/tableRecords"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:stretchColumns="*"/>

                <!-- Affichage si vide -->
                <TextView
                    android:id="@+id/tvEmpty"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="No records yet."
                    android:padding="12dp"
                    android:visibility="gone"/>
            </LinearLayout>
        </ScrollView>
    </HorizontalScrollView>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnAddRecord"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher_foreground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>